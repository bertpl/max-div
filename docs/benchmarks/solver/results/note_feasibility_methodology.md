For each benchmark size, the table below states the verdict of the feasibility diagnostic: **feasible** and **infeasible** are proofs (a witness selection, resp. a re-checkable certificate), **unknown** claims nothing. The **constraints-score ceiling** converts the certified violation floor onto the solver's 0-1 scale — no selection can score above it; feasible rows trivially show 1.0, unknown rows a dash.
