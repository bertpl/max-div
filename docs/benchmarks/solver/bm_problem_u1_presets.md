# Benchmark Results - Problem U1 - Solver Presets

!!! note "No measured results yet"
    `U1` is a new problem (introduced together with the n-based problem parametrization) and has
    no published preset results yet.  The other problems' pages show what these results look
    like once measured.
