# Capability Definitions

Every cell in the [comparison tables](comparison.md), the README capability table and the
per-solver [profiles](solvers/index.md) is pinned here to one exact criterion. The reasoning
behind each tool's own marks is on its profile page, and the procedure behind the measured
solver-scaling values is the [measurement protocol](scaling/protocol.md).

--8<-- "generated/capability_definitions.md"
