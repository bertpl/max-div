# Solver Scaling — Time

*Measurement pending.*

This page will publish, per tool, the largest grid size at which the tool returns a valid selection within the one-minute reference budget — alongside each tool's pinned fastest valid configuration and the measured per-size run outcomes. The definition and measurement method are pinned by the [protocol](protocol.md).
