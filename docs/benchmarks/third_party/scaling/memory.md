# Solver Scaling — Memory

*Measurement pending.*

This page will publish, per tool, the largest grid size whose peak memory stays within 32 GB in the tool's most memory-efficient configuration — alongside the fitted per-tool memory models and the measured peak-RSS points they are calibrated on. The definition and measurement method are pinned by the [protocol](protocol.md).
